{
  "name": "sunroom",
  "private": true,
  "version": "0.0.1",
  "description": "Senior-first family video calling app",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}
